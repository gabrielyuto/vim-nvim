return {
  {
    'neoclide/coc.nvim',
    branch = 'release',  -- Importante para garantir que você esteja na versão estável do plugin
    build = 'yarn install --frozen-lockfile',  -- ou 'npm install --frozen-lockfile'
  }
}
